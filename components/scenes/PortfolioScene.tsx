"use client";

import { motion } from "framer-motion";
import { portfolioItems } from "@/data/portfolio";

export function PortfolioScene(){return <section id="portfolio" className="portfolio-scene"><header><p className="eyebrow">05 / PORTFOLIO &amp; PROJECTS</p><h2>Intelligence made tangible.</h2><p>Company work, founder projects, research and confidentiality-safe client engagements—structured as an extensible portfolio system.</p></header><div className="portfolio-grid">{portfolioItems.map((item,index)=><motion.article className={`project-panel visual-${item.visual}`} key={item.slug} initial={{opacity:0,y:60}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.2}} transition={{duration:.7,delay:index*.06}}><div className="project-index">0{index+1}</div><div className="project-tags"><span>{item.ownership}</span><span>{item.category}</span></div><div className="project-visual" aria-hidden="true"><i/><i/><i/></div><h3>{item.title}</h3><p>{item.description}</p><dl><div><dt>TECHNOLOGIES</dt><dd>{item.technologies.join(" · ")}</dd></div><div><dt>STATUS</dt><dd>{item.status}</dd></div><div><dt>RESULT</dt><dd>{item.results}</dd></div></dl><a href={item.href} target={item.href.startsWith("http")?"_blank":undefined} rel={item.href.startsWith("http")?"noopener":undefined}>VIEW CASE STUDY <span>↗</span></a></motion.article>)}</div><p className="portfolio-boundary">Client names, private datasets, transaction terms, proprietary thresholds and unverified performance claims are never published.</p></section>}

// ─── Arima Universe — Auth Routes Layout ───
// All auth pages share this layout (no nav/footer).

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}